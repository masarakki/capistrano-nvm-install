require 'spec_helper'

describe Capistrano::Nvm::Install do
  it 'has a version number' do
    expect(Capistrano::Nvm::Install::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
